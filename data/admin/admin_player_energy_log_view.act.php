<?php
  $player_energy_log = Player_Energy_Log::instance( getValue('id') );

  if(!is_null(getValue('action'))) {
    switch( getValue('action') ) {
       default:
        break;
    }
  }

  // CUSTOM

  //Custom content

  // /CUSTOM
